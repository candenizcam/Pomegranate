package modules.Layout

import com.badlogic.gdx.graphics.g2d.SpriteBatch
import modules.LcsModule.GetLcs
import modules.LcsModule.LcsVariable

/*
class TiledLayout(override var width: LcsVariable, override var height: LcsVariable): OmniLayout() {
    override var cX = GetLcs.byPixel(0f)
    override var cY = GetLcs.byPixel(0f)


    override fun relocate(x: LcsVariable, y: LcsVariable) {
        cX = x
        cY = y
    }




}

 */